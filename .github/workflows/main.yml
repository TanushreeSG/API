name: CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Katalon Studio Github Action
      uses: katalon-studio/katalon-studio-github-action@v2
      with:
          version: '8.6.5'
          projectPath: '${{ github.workspace }}'
          args: 'katalonc -noSplash -runMode=console -projectPath="C:\Users\Tanushree SG\Desktop\demo\Pi API testing\Pi API testing.prj" -retry=0 -testSuitePath="Test Suites/chain" -browserType="Web Service" -executionProfile="default" -apiKey="f19072aa-b30b-4717-a469-84c7f8755dd0" --config -proxy.auth.option=NO_PROXY -proxy.system.option=NO_PROXY -proxy.system.applyToDesiredCapabilities=true -webui.autoUpdateDrivers=true'
